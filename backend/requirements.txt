# Requires Python 3.12, 3.13 or 3.14. Verified on 3.13 and 3.14.

# Web server
flask==3.1.3
flask-cors==6.0.5

# NBA data source
nba_api==1.11.4

# Data / numerics
pandas==2.3.3
numpy==2.3.5
scipy==1.18.0

# Machine learning
scikit-learn==1.9.0
xgboost==3.4.1
lightgbm==4.7.0

# Caching, parallelism, retries
joblib==1.5.3
retrying==1.4.2

# Production WSGI server (unused locally -- `python server.py` runs Flask's
# dev server; hosts start the app with `gunicorn server:app` instead).
gunicorn==23.0.0

# Python version note:
#   Verified working on Python 3.13.5 and 3.14.2, both with pandas 2.3.3
#   (which ships cp314 wheels). Vercel offers 3.12, 3.13 and 3.14; .python-version
#   pins 3.13, and you can change it to 3.14 to match a local 3.14 venv.
#
#   Stay on the pandas 2.x line. Under pandas 3.0.5 the /api/predict route fails
#   in predictive_model.py with "Invalid value '[...]' for dtype 'float64'" --
#   the array assignment in create_features is not valid there. Since pandas 3.0
#   is the only line with 3.14 wheels for *some* releases, pin the version here
#   rather than letting pip resolve it.
