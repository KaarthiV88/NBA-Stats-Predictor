# Requires Python 3.12 or 3.13 (see note at bottom -- 3.14 is NOT supported).

# Web server
flask==3.1.3
flask-cors==6.0.5

# NBA data source
nba_api==1.11.4

# Data / numerics
pandas==2.3.3
numpy==2.3.5
scipy==1.18.0

# Machine learning
scikit-learn==1.9.0
xgboost==3.4.1
lightgbm==4.7.0

# Caching, parallelism, retries
joblib==1.5.3
retrying==1.4.2

# Production WSGI server (unused locally -- `python server.py` runs Flask's
# dev server; hosts start the app with `gunicorn server:app` instead).
gunicorn==23.0.0

# Python version note:
#   pandas ships no wheels for Python 3.14 below pandas 3.0, and this project's
#   prediction code is not compatible with pandas 3.x -- under 3.0.5 the
#   /api/predict route fails in predictive_model.py:218 with
#   "Invalid value '[...]' for dtype 'float64'". Stay on Python 3.12/3.13
#   with pandas 2.3.x until that assignment is updated.
